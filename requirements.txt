# Pinned to match the original assignment's API surface (standalone `keras`
# package + `keras.backend.get_session`, `tf.compat.v1.logging`, etc).
# See the "Compatibility note" section of README.md before using a newer
# TensorFlow/Keras — several calls here (K.get_session, model.fit_generator-
# style generators) are from the TF1/standalone-Keras era.
numpy>=1.19,<2.0
pandas>=1.1,<2.0
seaborn>=0.11
matplotlib>=3.3
opencv-python>=4.5
scikit-learn>=0.24
pyyaml>=5.4
tensorflow>=2.3,<2.11
keras>=2.4,<2.11
